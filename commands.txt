# npm init 

# npm i express dotenv mongoose colors 
colors is for colors in the console

# npm i -D nodemon
nodemon as a dev dependency

# npm run server - with nodemon 

# git init 
# git add .
# git commit -m 'Initial commit'

to use async error handler.
# npm install express-async-handler

# git add .
# git commit -m 'Goals controller and routers setup.' 

// mongodb connection would be done through the cloud mongo atlas of chamaldeshitha2001@gmail.com

------------------------ 2nd video --------------------------------------------

install bcrypt to encrypt the passwords and jsonwebtoken to use
# npm i bcryptjs jsonwebtoken

// when we create an env variable it is a good practice to restart the server manually.

there was an error while sending the request from the postman with the login token for getting me endpoint.